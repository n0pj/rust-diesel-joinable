[package]
edition = "2021"
name = "rust-diesel-joinable"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
diesel = {version = "1.4.4", features = ["mysql"]}
dotenv = "^0.13.0"
mysql = "*"
